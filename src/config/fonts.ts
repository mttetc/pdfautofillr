// Vite doesn't support next/font, so we use CSS variables approach
// Add Inter font via CDN in index.html if needed

export const fontSans = {
  variable: "--font-sans",
};

export const fontMono = {
  variable: "--font-mono",
};
